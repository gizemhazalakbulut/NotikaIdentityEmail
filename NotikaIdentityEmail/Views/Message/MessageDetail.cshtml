@model Message

@{
    ViewData["Title"] = "MessageDetail";
    Layout = "~/Views/UserLayout/Index.cshtml";
}
@{
    var timeDiff = DateTime.Now - Model.SendDate; //Şuanın tarihi ile mesajın gönderildiği tarih arasındaki farkı hesapla
    string timeAgoText; // Zaman farkını göstermek için kullanılacak metin

    if (timeDiff.TotalMinutes < 1) // Eğer fark 1 dakikadan az ise
    {
        timeAgoText = "Şimdi";
    }
    else if (timeDiff.TotalMinutes < 60) // Eğer fark 1 saatten az ise
    {
        timeAgoText = $"{(int)timeDiff.TotalMinutes} dakika önce";
    }
    else if (timeDiff.TotalHours < 24) // Eğer fark 1 günden az ise
    {
        timeAgoText = $"{(int)timeDiff.TotalHours} saat önce";
    }

    else if (timeDiff.TotalDays < 7) // Eğer fark 1 haftadan az ise
    {
        timeAgoText = $"{(int)timeDiff.TotalDays} gün önce";
    }
    else
    {
        timeAgoText = Model.SendDate.ToString("dd MMMM yyyy"); // Eğer fark 1 haftadan fazla ise, tam tarihi göster
    }

}

<div class="inbox-area">
    <div class="container">
        <div class="row">

            @await Component.InvokeAsync("_MessageSidebarComponentPartial")

            <div class="col-lg-9 col-md-9 col-sm-9 col-xs-12">
                <div class="view-mail-list sm-res-mg-t-30">
                    <div class="view-mail-hd">
                        <div class="view-mail-hrd">
                            <h2>Mesaj Detayları</h2>
                        </div>
                        <div class="view-ml-rl">
                            <p>@Model.SendDate.ToString("HH:mm") (<span style="color:#6c757d">@timeAgoText</span>)</p>
                        </div>
                    </div>
                    <div class="mail-ads mail-vw-ph">
                        <p class="first-ph"><b>Konu: </b>@Model.Subject</p>
                        <p><b>Gönderici:</b> <a href="#">@Model.SenderEmail</a></p>
                        <p class="last-ph"><b>Tarih:</b> @Model.SendDate.ToString("dd MMMM yyyy")</p>
                    </div>
                    <div class="view-mail-atn">
                        <p>@Html.Raw(Model.MessageDetail)</p>
                    </div>

                    <div class="vw-ml-action-ls text-right mg-t-20">
                        <div class="btn-group ib-btn-gp active-hook nk-email-inbox">
                            <button class="btn btn-default btn-sm waves-effect"><i class="notika-icon notika-next"></i> Yanıtla</button>
                            <button class="btn btn-default btn-sm waves-effect"><i class="notika-icon notika-right-arrow"></i> Yönlendir</button>
                            <button class="btn btn-default btn-sm waves-effect"><i class="notika-icon notika-print"></i> Yazdır</button>
                            <button class="btn btn-default btn-sm waves-effect"><i class="notika-icon notika-trash"></i> Sil</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>